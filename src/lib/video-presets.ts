import type { VideoLandingConfig } from "@/components/video-landing";

export const VIDEO_PRESETS: Record<string, VideoLandingConfig> = {
  home: {
    title: "Video exclusivo",
    videoUrl: "",
    whatsappMessage: "Hola, quiero ser parte después de ver el video de Gary Mayhua.",
  },
};
